version: 0.2

phases:
 
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
      - echo  $(docker --version)
      - echo $(aws ecr get-login-password --region eu-central-1 |sudo  docker login --username AWS --password-stdin 353641719040.dkr.ecr.eu-central-1.amazonaws.com)
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
